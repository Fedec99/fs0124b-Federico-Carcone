// Questo file può essere sostituito durante la compilazione utilizzando l'array `fileReplacements`.
// `ng build` sostituisce `environment.ts` con `environment.prod.ts`.
// L'elenco delle sostituzioni dei file può essere trovato in `angular.json`.

export const environment = {
  production: false,
  apiURL: 'http://localhost:4201',
  movieApi: 'http://localhost:4201/movies-popular',
};

/*
 * Per un debug più semplice in modalità sviluppo, puoi importare il seguente file
 * per ignorare gli stack frame di errore relativi alla zona come `zone.run`, `zoneDelegate.invokeTask`.
 *
 * Questa importazione dovrebbe essere commentata in modalità produzione perché avrà un impatto negativo
 * sulle prestazioni se viene generato un errore.
 */
// importa 'zone.js/plugins/zone-error'; // Incluso con Angular CLI.

//Dovevo lavorare tutto la settimana quindi se questo commento è ancora qui allora non sono riuscito a committare/finire altro
