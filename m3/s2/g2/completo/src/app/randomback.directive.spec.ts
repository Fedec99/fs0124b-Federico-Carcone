import { RandombackDirective } from './randomback.directive';

describe('RandombackDirective', () => {
  it('should create an instance', () => {
    const directive = new RandombackDirective();
    expect(directive).toBeTruthy();
  });
});
